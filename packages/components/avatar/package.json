{
  "name": "@nextri-org/avatar",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "prepare": "panda codegen --clean",
    "debug": "panda debug",
    "build": "tsup --dts",
    "build-fast": "tsup --no-dts",
    "dev": "pnpm build-fast --watch",
    "test": "vitest"
  },
  "dependencies": {
    "@nextri-org/preset": "workspace:*",
    "@nextri-org/styco": "workspace:*"
  },
  "devDependencies": {
    "@storybook/react": "^7.2.3"
  }
}
